apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
spec:
  type: LoadBalancer # This creates an AWS Classic/Network Load Balancer
  selector:
    app: portfolio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
